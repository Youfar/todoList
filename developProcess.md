# First step: Install ruby and rails
1.install ruby

    curl -sSL https://get.rvm.io | bash -s stable
    source ~/.rvm/scripts/rvm
    rvm -v
    rvm install 2.3.0
2.install rails

    gem install rails
    rails -v
